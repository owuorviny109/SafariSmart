# Core Django
Django==5.0.1
python-decouple==3.8
Pillow>=10.0.0                  # Use latest compatible version

# AI Integration
google-generativeai==0.3.2

# Security
django-axes==6.1.1              # Brute force protection
django-csp==3.8                 # Content Security Policy
argon2-cffi==23.1.0             # Argon2 password hashing
django-ratelimit==4.1.0         # Additional rate limiting
bleach==6.1.0                   # HTML sanitization
django-cors-headers==4.3.1      # CORS protection

# Database (Production)
psycopg[binary]==3.2.3          # PostgreSQL adapter (Python 3.13 compatible)

# Development
django-debug-toolbar==4.2.0

# Production Server
gunicorn==21.2.0
whitenoise==6.6.0               # Static file serving
dj-database-url==2.1.0          # Database URL parsing
